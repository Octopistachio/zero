global.pause = false;

with(oUnpause)
    instance_destroy();
with(oPauseTitle)
    instance_destroy();
with(oMenuButton)
    instance_destroy();
//with(oRestartButton)
    //instance_destroy();
with(oContinueButton)
    instance_destroy();
with(oMuteMusic)
    instance_destroy();
with(oMute)
    instance_destroy()
